# 🍽️ AI Food Recognition System

سیستم تشخیص غذا با هوش مصنوعی - یک پروژه کامل شامل بک‌اند Django و فرانت‌اند React

## 📋 فهرست مطالب

- [ویژگی‌ها](#ویژگی‌ها)
- [ساختار پروژه](#ساختار-پروژه)
- [نصب و راه‌اندازی](#نصب-و-راه‌اندازی)
- [استفاده](#استفاده)
- [API ها](#api-ها)
- [آموزش مجدد مدل](#آموزش-مجدد-مدل)
- [مشکلات رایج](#مشکلات-رایج)
- [مشارکت](#مشارکت)

## ✨ ویژگی‌ها

### 🤖 هوش مصنوعی
- تشخیص نوع غذا با استفاده از مدل EfficientNet
- دقت بالا در تشخیص انواع مختلف غذا
- قابلیت یادگیری مداوم با فیدبک کاربران

### 🌐 وب اپلیکیشن
- **بک‌اند**: Django REST Framework
- **فرانت‌اند**: React با Material UI
- **دیتابیس**: SQLite (قابل تغییر به PostgreSQL)
- **احراز هویت**: سیستم توکن برای ویرایش فیدبک‌ها

### 📱 رابط کاربری
- طراحی مدرن و ریسپانسیو
- ناوبری آسان بین صفحات
- فرم‌های زیبا و کاربرپسند
- نمایش نتایج با انیمیشن‌های جذاب

### 🔧 مدیریت
- مدیریت لیبل‌های غذا
- مشاهده و ویرایش فیدبک‌ها
- آمار و گزارش‌گیری
- آموزش مجدد خودکار مدل

## 📁 ساختار پروژه

```
AI_Food/
├── backend/                 # بک‌اند Django
│   ├── ai_api/             # اپ اصلی
│   │   ├── models.py       # مدل‌های دیتابیس
│   │   ├── views.py        # API views
│   │   ├── serializers.py  # Django REST serializers
│   │   └── urls.py         # URL patterns
│   ├── media/              # فایل‌های آپلود شده
│   ├── manage.py           # Django management
│   └── settings.py         # تنظیمات Django
├── frontend/               # فرانت‌اند React
│   ├── src/
│   │   ├── components/     # کامپوننت‌های React
│   │   ├── forms/          # فرم‌ها
│   │   ├── pages/          # صفحات
│   │   ├── services/       # سرویس‌های API
│   │   └── App.js          # کامپوننت اصلی
│   └── package.json        # وابستگی‌های React
├── retrain_model.py        # اسکریپت آموزش مجدد
├── test_6.py              # مدل اصلی PyTorch
├── engine.py              # موتور تشخیص
└── README.md              # این فایل
```

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها
- Python 3.8+
- Node.js 14+
- pip
- npm یا yarn

### 1. کلون کردن پروژه
```bash
git clone <repository-url>
cd AI_Food
```

### 2. راه‌اندازی بک‌اند
```bash
# نصب وابستگی‌های Python
pip install -r requirements.txt

# یا نصب دستی
pip install django djangorestframework django-cors-headers pillow torch torchvision

# مهاجرت دیتابیس
cd backend
python manage.py makemigrations
python manage.py migrate

# ایجاد کاربر ادمین
python manage.py createsuperuser

# اجرای سرور
python manage.py runserver
```

### 3. راه‌اندازی فرانت‌اند
```bash
# نصب وابستگی‌های React
cd frontend
npm install

# یا با yarn
yarn install

# اجرای سرور توسعه
npm start
```

### 4. تنظیم فایل config
فایل `frontend/src/confige.json` را ویرایش کنید:
```json
{
  "URL": "http://localhost:8000"
}
```

## 📖 استفاده

### 1. تشخیص غذا
1. به صفحه "تشخیص غذا" بروید
2. عکس غذای خود را آپلود کنید
3. نتیجه تشخیص را مشاهده کنید
4. در صورت اشتباه بودن، لیبل صحیح را انتخاب کنید

### 2. مدیریت فیدبک‌ها
1. به صفحه "لیست فیدبک‌ها" بروید
2. فیدبک‌های ثبت شده را مشاهده کنید
3. با استفاده از کد دسترسی، فیدبک‌ها را ویرایش کنید

### 3. مدیریت لیبل‌ها
1. به صفحه "مدیریت لیبل‌ها" بروید
2. لیبل‌های جدید اضافه کنید
3. لیبل‌های موجود را ویرایش یا حذف کنید

## 🔌 API ها

### تشخیص غذا
```
POST /api/food/predict/
Content-Type: multipart/form-data

Parameters:
- image: فایل عکس
- correct_label: ID لیبل صحیح (اختیاری)
```

### لیست فیدبک‌ها
```
GET /api/food/feedback-list/
```

### ویرایش فیدبک
```
PATCH /api/food/feedback/{id}/edit/
Content-Type: multipart/form-data

Parameters:
- image: فایل عکس جدید (اختیاری)
- label_id: ID لیبل جدید (اختیاری)
- token: کد دسترسی
```

### مدیریت لیبل‌ها
```
GET    /api/food/labels/           # لیست لیبل‌ها
POST   /api/food/labels/           # ایجاد لیبل جدید
PUT    /api/food/labels/{id}/      # ویرایش لیبل
DELETE /api/food/labels/{id}/      # حذف لیبل
```

### آموزش مجدد مدل
```
POST /api/food/admin/retrain/
```

## 🤖 آموزش مجدد مدل

### اجرای دستی
```bash
python retrain_model.py
```

### زمان‌بندی خودکار

#### Windows (Task Scheduler)
1. Task Scheduler را باز کنید
2. Create Basic Task
3. نام: "AI Food Model Retrain"
4. Trigger: Daily
5. Action: Start a program
6. Program: `python`
7. Arguments: `retrain_model.py`
8. Start in: مسیر پروژه

#### Linux/Mac (Cron)
```bash
# ویرایش crontab
crontab -e

# اضافه کردن خط زیر برای اجرا هر روز ساعت 2 صبح
0 2 * * * cd /path/to/AI_Food && python retrain_model.py
```

## 🔧 مشکلات رایج

### خطای CORS
```python
# در backend/settings.py
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
    "http://127.0.0.1:3000",
]
```

### خطای Proxy در React
```json
// در frontend/package.json
{
  "proxy": "http://localhost:8000"
}
```

### خطای Migration
```bash
# پاک کردن migration ها
python manage.py migrate ai_api zero
rm backend/ai_api/migrations/0*.py

# ایجاد migration جدید
python manage.py makemigrations
python manage.py migrate
```

### خطای PyTorch
```bash
# نصب PyTorch
pip install torch torchvision

# یا برای CUDA
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
```

## 📊 آمار و گزارش‌گیری

پروژه شامل سیستم گزارش‌گیری کامل است:
- تعداد کل نمونه‌ها
- تعداد نمونه‌های دارای لیبل
- توزیع لیبل‌ها
- دقت مدل
- تاریخ آخرین آموزش

## 🤝 مشارکت

1. Fork کنید
2. Branch جدید بسازید (`git checkout -b feature/amazing-feature`)
3. تغییرات را commit کنید (`git commit -m 'Add amazing feature'`)
4. Push کنید (`git push origin feature/amazing-feature`)
5. Pull Request بسازید

## 📝 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

## 📞 پشتیبانی

برای سوالات و مشکلات:
- Issue در GitHub ایجاد کنید
- ایمیل: [your-email@example.com]

---

**نکته**: این پروژه برای اهداف آموزشی و تحقیقاتی طراحی شده است. برای استفاده تجاری، لطفاً با تیم توسعه تماس بگیرید. 